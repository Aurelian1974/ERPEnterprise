# Persoane - Validation UI Improvements (2026-01-13)

## Summary
- Implemented inline visual feedback for form validation in the Persoane add/edit dialog.
- Integrated CNP custom validation into the EditContext so validation messages appear through the standard Blazor ValidationMessage component.
- Ensured inputs get `is-invalid` class when fields have validation errors so styles (red border / message) apply consistently.

## Files changed
- `Components/Pages/Administrare/Persoane.razor`
  - Updated `SfTextBox` inputs to use `CombineFieldCss(editContext, p, nameof(Persoana.X))` so validation classes are applied.
  - Wired CNP input `@oninput` to call `ValidateCnpField(p.CNP, editContext)`.
  - Removed duplicate custom CNP span error (validation now uses ValidationMessage component).

- `Components/Pages/Administrare/Persoane.razor.cs`
  - Added `CombineFieldCss(EditContext?, object, string)` helper to combine default FieldCssClass with an `is-invalid` marker when the field has validation messages.
  - Updated `ValidateCnpField` to accept `EditContext?` and use `ValidationMessageStore` to add/remove validation messages for the CNP field and call `NotifyValidationStateChanged()`.
  - Added `using Microsoft.AspNetCore.Components.Forms;`.

- `Components/Pages/Administrare/Persoane.razor.css`
  - Existing styles were used; no changes required. CSS already includes `.validation-message` and `.is-invalid` rules.

## Notes
- Validation is driven by DataAnnotations + FluentValidation (PersoanaValidator). The CNP validation is custom and now integrates with the EditContext so the UI displays it consistently with other validation errors.
- Build: `dotnet build` succeeded locally after changes.

## Next steps
- Manual UI verification in browser: open `/administrare/persoane`, try adding/editing a person and intentionally leave `Nume`/`Prenume` empty or enter invalid CNP to confirm red borders and messages are shown.
- If desired, add component tests that exercise the EditForm behavior (bUnit) to assert validation state changes.

---

Generated by GitHub Copilot (automated changes - see PR for diff).